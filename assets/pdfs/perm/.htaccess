 <IfModule mod_expires.c>
   # Enable expirations.
   ExpiresActive On

   # Cache all files for 2 weeks after access (A).
   ExpiresDefault A0

  <FilesMatch (\.pdf)$>
     ExpiresActive Off
  </FilesMatch>
 </IfModule>